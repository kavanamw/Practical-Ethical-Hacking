Pass the Password/Hash

* If we crack a password or has we can leverage both for lateral movement in the network 
	* use crackmapexec to pass those hashes around the network 
	* can get a hash dump in metasploit 
	* maybe the entire network uses the same network or hash meaning your already done 
	* 